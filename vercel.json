{
  "buildCommand": "npm install --production=false && npm install tailwindcss-animate --save && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install"
}